           self.log("📦 Paketler yükleniyor...")
            self.progress.setValue(1)
            self.configure_kerberos_and_samba(domain_name, host_name)


                def configure_kerberos_and_samba(domain_name, host_name):
        """ad.sh betiğini çalıştırarak Kerberos ve Samba yapılandırmasını yapar."""
        script_path = "ad.sh"  # Script'in yolu
    
        if not os.path.exists(script_path):
            print(f"❌ Hata: {script_path} dosyası bulunamadı!")
            return
    
        try:
            command = ["bash", script_path, domain_name, host_name]
            result = subprocess.run(command, stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)

            if result.returncode == 0:
                print("✅ Kerberos ve Samba yapılandırması başarılı!")
                print(result.stdout)
            else:
                print("❌ Yapılandırma başarısız:\n" + result.stderr)
    
        except Exception as e:
            print(f"❌ Bir hata oluştu: {str(e)}")


